# نقل بكج متابعة الحمل إلى GitHub وCloudflare

## ما تحتويه الحزمة

- كود المصدر الكامل للموقع: `app/`, `db/`, `public/`, `drizzle/` وبقية ملفات المشروع.
- نسخة JSON وSQL من قاعدة البيانات داخل `database-export/`.
- قالب إعداد Cloudflare: `wrangler.toml.example`.

## نقطة مهمة عن الاستضافة

GitHub مناسب لحفظ الكود وتشغيل النشر التلقائي، لكنه ليس مكان تشغيل النظام نفسه. الموقع يحتاج Cloudflare Workers لأنه يحتوي على واجهات API وقاعدة D1 وصلاحيات المستخدمين.

التوزيع الصحيح هو:

1. إنشاء مستودع **خاص Private** في GitHub ورفع محتويات هذه الحزمة إليه.
2. ربط المستودع بحساب Cloudflare Workers.
3. إنشاء قاعدة D1 جديدة وإضافة معرفها إلى `wrangler.toml`.
4. استيراد المخطط والبيانات من مجلد `database-export`.
5. نشر Worker من Cloudflare أو GitHub Actions.

## المصادقة (بعد التعديل)

- **الطبيبة:** تسجيل دخول برمز تحقق (OTP) يُرسل على بريدها الرسمي `@madlouh.com.sa` عبر بوابة `hostgator-otp-gateway` الموجودة. الجلسة كوكي موقّعة (HMAC)، صلاحيتها 14 يوم.
- **المريضة:** بدون حساب أو تسجيل دخول. تصل لملفها للقراءة فقط عبر رابط يحتوي رمز الملف: `/p/<رمز الملف>`.

يلزم ضبط هذه الأسرار قبل النشر (`wrangler secret put`):
- `SETUP_CODE` — رمز تفعيل حساب الطبيبة الأول
- `SESSION_SECRET` — مفتاح عشوائي طويل (32 حرف فأكثر) لتوقيع الجلسة
- `OTP_GATEWAY_API_KEY` — نفس المفتاح المضبوط في `otp-config.php` على `madlouh.com.sa`

## ماذا لا تتضمنه الحزمة

- لا تتضمن أي كلمات مرور أو أسرار أو مفاتيح API.
- لا تتضمن إعدادات الاستضافة الحالية الخاصة بمنصة ChatGPT.
- لا تتضمن بوابة OTP في HostGator؛ هذه تبقى مستقلة في `madlouh.com.sa/otp-api`.
